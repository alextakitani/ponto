<%# Create inválido: re-renderiza a seção com a task errada no form (reexibe os erros
    inline — colisão de nome, arquivado etc.). Ver `_section_stream`. %>
<%= render "projects/tasks/section_stream", project: @project, form_task: @task %>
