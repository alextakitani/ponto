<%# Task deletada: re-renderiza a seção (a linha some; empty state reaparece se era a
    última). @project foi setado no destroy antes de apagar a task. Ver `_section_stream`. %>
<%= render "projects/tasks/section_stream", project: @project %>
