<% content_for :title, t(".title") %>

<header class="page-header">
  <div>
    <h1><%= t(".title") %></h1>
    <p class="muted"><%= t(".lede") %></p>
  </div>
  <%= link_to new_client_path, class: "btn" do %>
    <%= icon("plus") %>
    <%= t(".new") %>
  <% end %>
</header>

<div class="page-toolbar">
  <%# Busca por nome (filtra EM RUBY no controller — name é criptografado). %>
  <%= form_with url: clients_path, method: :get, class: "page-search" do |form| %>
    <%= icon("search") %>
    <% if @showing_archived %><%= form.hidden_field :archived, value: "1" %><% end %>
    <%= form.search_field :q, value: params[:q], placeholder: t("common.search_by_name"),
          autocomplete: "off", "aria-label": t(".search_aria") %>
  <% end %>

  <%# Alterna entre ativos e arquivados (link discreto — Q7). %>
  <% if @showing_archived %>
    <%= link_to t("common.view_active"), clients_path, class: "muted" %>
  <% else %>
    <%= link_to t("common.view_archived"), clients_path(archived: "1"), class: "muted" %>
  <% end %>
</div>

<% if @clients.any? %>
  <table class="data-table">
    <thead>
      <tr>
        <th><%= t("common.labels.name") %></th>
        <th><%= t("common.labels.currency") %></th>
        <th class="data-table__num"><%= t("common.labels.rate_short") %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <%= render partial: "clients/client", collection: @clients, as: :client %>
    </tbody>
  </table>
<% elsif params[:q].present? %>
  <div class="empty-state">
    <p><%= t(".empty.search", query: params[:q]) %></p>
  </div>
<% elsif @showing_archived %>
  <div class="empty-state">
    <p><%= t(".empty.archived") %></p>
  </div>
<% else %>
  <%# Empty state convidativo — primeira coisa que o usuário vê. %>
  <div class="empty-state">
    <%= icon("users", class: "icon--lg") %>
    <h2><%= t(".empty.title") %></h2>
    <p class="muted">
      <%= t(".empty.description") %>
    </p>
    <%= link_to new_client_path, class: "btn" do %>
      <%= icon("plus") %>
      <%= t(".empty.cta") %>
    <% end %>
  </div>
<% end %>
